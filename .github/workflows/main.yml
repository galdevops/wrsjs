name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  run_main:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Installing dependencies
      run: |
        ls
      shell: bash
